#include "monty.h"
/**
 *pint - doesn’t do anything
 *@stack: a pointer to the first node in the stack
 *@line_number: the line number
 */
void pint(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
	return;
}
